import Hero from "../Components/Hero";

function Home() {
    return (
        <div>
            <Hero />
            {/* Add other sections like Services, Testimonials, etc. */}
        </div>
    );
}

export default Home;
